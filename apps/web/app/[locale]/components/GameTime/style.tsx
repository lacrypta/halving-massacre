import { styled } from 'styled-components';
import { appTheme } from '../../../../config/exports';

export const GameTimeStyle = styled.div`
  height: 60px;

  background-color: ${appTheme.colors.gray20};
`;
